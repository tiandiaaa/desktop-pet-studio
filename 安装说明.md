# 桌宠工坊 · 安装说明

桌宠工坊生成桌宠，需要两样「大脑」，按需安装即可：

| 组件 | 用途 | 是否必需 | 大小 |
|------|------|---------|------|
| **本地模型 Ollama** | 对话 + 看屏（理解你的屏幕） | 必需（或改用 API） | 约 11GB |
| **GPT-SoVITS 整合包** | 语音合成（让桌宠说话） | 可选（不装则桌宠只打字不发声） | 约 7GB |

---

## 一、本地模型 Ollama（对话 + 看屏）

### 1. 下载安装
官网：**https://ollama.com/download**
（国内访问慢时，可用加速器，或搜索「Ollama 国内镜像下载」）

### 2. 拉取模型
安装完成后，打开命令行（Win+R 输入 `cmd`），执行：

```
ollama pull qwen2.5vl:7b
```

这个模型既能聊天、又能看屏幕（多模态），约 5.5GB。

### 3. 确认
在桌宠工坊「环境设置」里，确认服务地址为：
```
http://localhost:11434
```
状态显示「✓ 在线」即可。

---

## 二、GPT-SoVITS 整合包（语音合成，可选）

### 1. 下载
国内镜像（推荐）：**https://hf-mirror.com/lj1995/GPT-SoVITS-windows-package**
文件：`GPT-SoVITS-v4-20250529.7z`（约 7GB）

### 2. 解压放置
用 7-Zip 解压，得到 `GPT-SoVITS-v4-20250529/` 文件夹，**放在任意位置**（建议空间充足的盘，解压后约 13GB）。

### 3. 配置路径
在桌宠工坊「环境设置」里，点「浏览」选择解压后的目录——
**注意要选包含 `api.py` 的那一层**（即 `GPT-SoVITS-v4-20250529/` 这一层）。
状态显示「✓ 已就绪」即可。

### 4. 准备参考音频
语音用「零样本克隆」，无需训练。准备一段 **3~10 秒**的目标音色音频（wav 格式），
在向导第三步提交即可，几分钟就能用该音色说话。

---

## 三、不想装本地模型？改用 API

在向导第四步「AI 引擎」选择 **API（DeepSeek 等）**，填写：
- Base URL（如 `https://api.deepseek.com/v1`）
- API Key
- 模型（如 `deepseek-chat`）

> 注意：DeepSeek 暂无视觉 API，此时「看屏」功能会失效，仅保留文字对话。看屏需要配支持图像的模型（如智谱 glm-4v、通义 qwen-vl）。

---

## 四、常见问题

**Q：桌宠没声音？**
A：GPT-SoVITS 整合包没装或路径没配好；或没提交参考音频。检查「环境设置」状态是否为「✓ 已就绪」。

**Q：双击启动没反应？**
A：确认已安装 Python 3.10+ 并装了依赖（PySide6、Pillow、requests、numpy、pywin32），或桌宠工坊的 Python 路径配置正确。

**Q：看屏很慢/卡顿？**
A：本地 7B 视觉模型对显卡有要求（建议 8GB 显存）。可换小模型 `qwen2.5vl:3b`，或用 API 视觉模型。
